 <%= simple_form_for [@project, @project_member] do |f| %>
      <%= f.input :user.name,  as: :select, collection: User.all.map { |u| [u.name, u.id] }, label: "Usuário" %>
      <%= f.input :user_type, label: "User Type" %>
      <%= f.button :submit, class:'btn btn-primary' %>
    <% end %>
